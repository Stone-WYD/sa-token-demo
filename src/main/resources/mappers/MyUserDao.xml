<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wyd.satokendemospringboot.demos.dao.MyUserDao">

    <resultMap type="com.wyd.satokendemospringboot.demos.entity.MyUser" id="MyUserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="userPassword" column="user_password" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="enable" column="enable" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MyUserMap">
        select
          id, user_name, user_password, age, enable
        from my_user
        where id = #{id}
    </select>


    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MyUserMap">
        select
          id, user_name, user_password, age, enable
        from my_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="userPassword != null and userPassword != ''">
                and user_password = #{userPassword}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="enable != null">
                and enable = #{enable}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from my_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="userPassword != null and userPassword != ''">
                and user_password = #{userPassword}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="enable != null">
                and enable = #{enable}
            </if>
        </where>
    </select>
    <select id="queryByNameAndPassword" resultMap="MyUserMap">
        select
            id, user_name, user_password, age, enable
        from my_user
        where user_name = #{userName}
        and user_password = #{password}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into my_user(user_name, user_password, age, enable)
        values (#{userName}, #{userPassword}, #{age}, #{enable})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into my_user(user_name, user_password, age, enable)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userName}, #{entity.userPassword}, #{entity.age}, #{entity.enable})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into my_user(user_name, user_password, age, enable)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.userPassword}, #{entity.age}, #{entity.enable})
        </foreach>
        on duplicate key update
        user_name = values(user_name),
        user_password = values(user_password),
        age = values(age),
        enable = values(enable)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update my_user
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="userPassword != null and userPassword != ''">
                user_password = #{userPassword},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="enable != null">
                enable = #{enable},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from my_user where id = #{id}
    </delete>

</mapper>

